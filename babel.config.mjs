export default {
  // Use modern JavaScript syntax targeting the current Node version.
  // No transforms are applied by default; Babel is only used so Jest can process
  // ESM and any future syntax consistently.
  presets: [],
};
